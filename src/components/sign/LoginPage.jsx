import React, { useState } from 'react';
import styles from './LoginPage.module.scss';

const LoginPage = () => {
  return <div className={styles.sign_in_page}>login</div>;
};

export default LoginPage;
